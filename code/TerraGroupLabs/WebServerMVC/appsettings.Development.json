{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ApiSettings": {
    "BaseUrl": "http://webapi:8080/api",
    "_comment": "The BaseUrl is the URL of the WebAPI service. The URL is the name of the service in the Docker network and the port number.",
    "_comment2": "Inside Docker, call the webapi service name at port 8080"
  }
}
